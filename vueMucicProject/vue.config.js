module.exports = {
    publicPath: "",//dist里面的文件路径变为相对路径 让dist可以直接用vscode的server(http://127.0.0.1:5500/)直接运行
    // devServer: {//配置了才能跨域 但是只能是测试阶段
    //     proxy: "https://suggest.taobao.com",
    // },
};